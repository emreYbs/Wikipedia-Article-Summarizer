#Requirements

#Install these as requirements if you need. 
#(you may also need to install nltk's stopwords package)

pip install beautifulsoup4
pip install lxml
pip install nltk 

#You may also try "pip3 install beautifulsoup4" if "pip" encounters errors.
